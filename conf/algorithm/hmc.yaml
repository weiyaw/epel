# Vanilla HMC, no adaptation
hmc:
  n_samples: 2000               # number of mcmc samples to draw
  n_warmup: 1000                # number of warmup steps before drawing samples
  num_integration_steps: 10
  step_size: 0.1
